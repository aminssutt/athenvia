{
  "name": "athenvia",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/with-env.mjs --parallel --filter @athenvia/web --filter @athenvia/worker dev",
    "dev:web": "node scripts/with-env.mjs --filter @athenvia/web dev",
    "dev:worker": "node scripts/with-env.mjs --filter @athenvia/worker dev",
    "build": "corepack pnpm -r --if-present build",
    "lint": "eslint .",
    "typecheck": "corepack pnpm -r --if-present typecheck",
    "test": "corepack pnpm -r --if-present test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:generate": "node scripts/with-env.mjs --filter @athenvia/database db:generate",
    "db:validate": "node scripts/with-env.mjs --filter @athenvia/database db:validate",
    "db:migrate": "node scripts/with-env.mjs --filter @athenvia/database db:migrate",
    "db:seed": "node scripts/with-env.mjs --filter @athenvia/database db:seed",
    "db:seed:check": "node scripts/with-env.mjs --filter @athenvia/database db:seed:check",
    "db:import:ror": "node scripts/with-env.mjs --filter @athenvia/database db:import:ror",
    "vapid:generate": "web-push generate-vapid-keys",
    "db:import:tmm": "node scripts/with-env.mjs --filter @athenvia/database db:import:tmm"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^16.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0",
    "web-push": "^3.6.7"
  }
}
